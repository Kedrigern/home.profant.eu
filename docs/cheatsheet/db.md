# DB

## SQL

## PostgreSQL

```bash
sudo -S -u postgres psql   # pq super user
```

```console 
$ pwd
$ sudo -S -u postgres psql   # pq super user
```

```shell-session 
$ sudo -S -u postgres psql   # pq super user
$ ls
```

```console
$ pwd
/home/user
$ cd test
$ ls -la
```

!!! note

    be car